# 알기사 모의고사 3장 정보보안기사 문제

| 항목 | 내용 |
|------|------|
| 성격 | 알기사 모의고사 3장 기반 검수본 |
| 확보 문항 | 37문항 |
| 확보 번호 | 23, 25, 29, 30, 34, 41, 44, 68, 78, 80, 88, 96, 107, 114, 122, 137, 140, 145, 147, 154, 166, 172, 174, 186, 190, 197, 209, 214, 220, 227, 230, 239, 240, 257, 709, 711, 755 |
| 작성 원칙 | 확보된 문항 번호를 보존하고, 오탈자·보기 번호 오류를 의미가 바뀌지 않는 범위에서 정정한다. |
| 주의 | 709번은 원문에 보기가 없어 문항 의미에 맞는 객관식 보기를 보강했다. |

## 4과목 정보보안 일반

### 23. DRM 구성 요소 중에서 콘텐츠를 이용하는 사용자에 대해 정해진 정책에 따라 사용 권한을 결정하고, 부여된 사용 권한에 따라 라이선스 발급 및 그 내역을 관리하는 시스템은 무엇인가?

1. 패키저
2. 클리어링 하우스
3. 시큐어 컨테이너
4. DRM 제어기

<details>
<summary>정답 보기</summary>

**정답**: 2

**근거**: 클리어링 하우스는 DRM에서 권한 정책에 따른 라이선스 발급과 사용 내역 관리를 담당한다.

</details>

### 25. 다음 중 단일 치환 암호를 해독할 수 있는 방법으로 알맞은 것은?

1. 빈도 분석법
2. 알파벳 분석법
3. 치환 분석법
4. 순서 분석법

<details>
<summary>정답 보기</summary>

**정답**: 1

**근거**: 단일 치환 암호는 평문 문자 빈도 특성이 암호문에 남기 때문에 빈도 분석의 대표 대상이다.

</details>

### 29. 256비트 길이의 AES 알고리즘 라운드 개수는?

1. 10
2. 12
3. 14
4. 16

<details>
<summary>정답 보기</summary>

**정답**: 3

**근거**: AES는 키 길이 128/192/256비트에 따라 각각 10/12/14라운드를 사용한다.

</details>

### 30. 아래 보기에서 설명하고 있는 공격 기법은?

> 1990년 Biham과 Shamir에 의하여 개발된 선택 평문 공격법으로, 두 개의 평문 블록의 비트 차이에 대해 대응되는 암호문 블록들의 비트 차이를 이용하여 사용된 암호키를 찾아내는 방법이다.

1. 차분 공격
2. 선형 공격
3. 전수 공격
4. 통계적 분석

<details>
<summary>정답 보기</summary>

**정답**: 1

**근거**: 평문 쌍의 입력 차이와 암호문 쌍의 출력 차이를 이용하는 선택 평문 공격은 차분 분석이다.

</details>

### 34. 블록 암호 알고리즘에서 최초의 평문 블록을 암호화할 때는 "한 단계 앞의 암호문 블록"이 존재하지 않으므로 이를 대신할 비트열을 무엇이라고 하는가?

1. 패딩
2. 초기화 벡터
3. 운영 모드
4. 암호문 블록

<details>
<summary>정답 보기</summary>

**정답**: 2

**근거**: CBC 등 블록 암호 운영 모드에서 첫 블록에 앞선 암호문 블록 역할을 하는 값은 IV, 즉 초기화 벡터다.

</details>

### 41. 스트림 암호 방식의 블록 암호 모드로만 구성된 것은?

1. ECB, CBC, CTR
2. CFB, OFB, CTR
3. CBC, CFB, OFB
4. ECB, CFB, CTR

<details>
<summary>정답 보기</summary>

**정답**: 2

**근거**: CFB, OFB, CTR은 블록 암호를 스트림 형태로 운용할 수 있는 대표 모드다.

</details>

### 44. KDC를 이용한 키 분배 방식에 대한 설명 중 옳지 않은 것은?

1. 사용자들은 사전에 KDC와 마스터 키를 공유해야 한다.
2. 사용자의 요청이 있는 경우 KDC는 일회용 세션키를 생성한다.
3. 사용자들 간에는 사전에 공유한 비밀정보가 필요하지 않다.
4. KDC는 일회용 세션키를 사용자의 공개키로 암호화하여 전송한다.

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: 전통적인 KDC 방식은 사용자와 KDC가 사전에 공유한 대칭키로 세션키를 안전하게 전달한다. 공개키 암호화 설명은 KDC 방식과 맞지 않는다.

</details>

### 68. 다음 중 전용 해시 함수에 대한 설명으로 옳지 않은 것은?

1. MD5는 16단계의 4라운드를 사용한다.
2. MD5와 SHA-1은 메시지를 512비트 블록 단위로 처리한다.
3. SHA-1은 128비트의 값을 출력한다.
4. SHA-1은 20단계의 4라운드를 사용한다.

<details>
<summary>정답 보기</summary>

**정답**: 3

**근거**: SHA-1의 해시 출력 길이는 160비트다. 128비트 출력은 MD5에 해당한다.

</details>

### 78. 전자서명의 요구조건에 관한 설명으로 틀린 것은?

1. 서명은 메시지에 의존하는 비트 형태여야 한다.
2. 위조와 부인방지를 위하여 수신자의 유일한 정보 비트를 이용해야 한다.
3. 서명문을 만들거나 인식, 확인하기 쉬워야 한다.
4. 기억장소에 서명문의 복사본을 유지하는 것이 실용적이어야 한다.

<details>
<summary>정답 보기</summary>

**정답**: 2

**근거**: 전자서명은 수신자가 아니라 서명자의 개인키 등 서명자 고유 정보를 기반으로 생성되어야 한다.

</details>

### 80. 다음에서 설명하는 내용에 해당되는 것은?

> 전자서명의 서명자를 누구든지 검증할 수 있어야 한다.

1. 위조 불가 조건
2. 부인 불가 조건
3. 서명자 인증 조건
4. 재사용 불가 조건

<details>
<summary>정답 보기</summary>

**정답**: 3

**근거**: 누구든지 서명자를 검증할 수 있어야 한다는 설명은 서명자 인증 조건과 직접 연결된다.

</details>

### 88. 전자서명을 적용한 예가 아닌 것은?

1. Code Signing
2. X.509 Certificate
3. SSL/TLS 프로토콜
4. Kerberos Protocol

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: Kerberos는 KDC 기반 대칭키·티켓 인증 프로토콜이며 전자서명 적용 사례로 보기 어렵다.

</details>

### 96. 다음 중 PKI의 주요 기능과 가장 거리가 먼 것은?

1. 인증
2. 가용성
3. 기밀성
4. 부인방지

<details>
<summary>정답 보기</summary>

**정답**: 2

**근거**: PKI는 인증, 기밀성, 무결성, 부인방지 등을 지원하지만 가용성은 주요 기능으로 분류하지 않는다.

</details>

### 107. 다음 중 전자 서명 인증서 폐지 목록(CRL)에 포함되어야 하는 기본 필드에 속하지 않는 것은?

1. 버전
2. 서명 알고리즘
3. 다음 발급 일자
4. 인증서 효력 정지 및 폐지 목록 번호

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: CRL 기본 필드에는 버전, 서명 알고리즘, 발급자, 갱신 시각, 다음 갱신 시각 등이 포함된다. CRL 번호는 확장 필드 성격이다.

</details>

### 114. 접근통제 단계 중 인가에 대한 설명으로 옳은 것은?

1. 인증 서비스에 스스로를 확인시키기 위해 정보를 공급하는 주체의 활동이다.
2. 접근 매체로는 비밀번호, 스마트 카드, 홍채, 음성 등이 있다.
3. 접근하고자 하는 사용자가 본인이 맞다는 것을 시스템이 인정해주는 활동이다.
4. 접근 제어 수단으로는 접근 제어 목록(ACL), 보안 레이블 등이 있다.

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: 인가는 인증된 주체가 어떤 객체에 어떤 권한으로 접근할 수 있는지를 결정하며 ACL과 보안 레이블이 대표 수단이다.

</details>

### 122. 다음 공격도구들이 공통적으로 제공하는 공격 기능은?

> John the Ripper, Wfuzz, Cain and Abel, THC Hydra

1. Brute Force 패스워드 공격
2. Web 공격
3. 사회 공학 공격
4. 스니핑 공격

<details>
<summary>정답 보기</summary>

**정답**: 1

**근거**: 나열된 도구들은 계정·패스워드 추측, 크래킹, 무차별 대입 공격과 관련된다.

</details>

### 137. 다음 중 커버로스의 세 가지 요소에 속하지 않는 것은?

1. 사용자
2. 클라이언트
3. SSO
4. 서버

<details>
<summary>정답 보기</summary>

**정답**: 3

**근거**: Kerberos는 사용자/클라이언트, 서버, KDC 기반 구조로 이해한다. SSO는 Kerberos가 제공할 수 있는 효과이지 구성 요소가 아니다.

</details>

### 140. 다음 중 Kerberos 키 분배 프로토콜의 기반 기술에 해당하는 것은?

1. Needham-Schroeder 프로토콜
2. Challenge-Response 프로토콜
3. Diffie-Hellman 프로토콜
4. RSA 이용 키 분배 프로토콜

<details>
<summary>정답 보기</summary>

**정답**: 1

**근거**: Kerberos는 Needham-Schroeder 대칭키 기반 키 분배 구조를 개선한 티켓 기반 인증 프로토콜이다.

</details>

### 145. 강제적 접근통제 정책에 대한 설명으로 옳지 않은 것은?

1. 모든 주체와 객체에 보안관리자가 부여한 보안 레이블이 부여되며 주체가 객체에 접근할 때 주체와 객체의 보안 레이블을 비교하여 접근 허가 여부를 결정한다.
2. 미리 정의된 보안 규칙들에 의해 접근 허가 여부가 판단되므로 임의적 접근통제 정책에 비해 객체에 대한 중앙 집중적인 접근통제가 가능하다.
3. 강제적 접근통제 정책을 지원하는 대표적 접근통제 모델로는 BLP, Biba 등이 있다.
4. 강제적 접근통제 정책을 구현하는 대표적 보안 메커니즘으로 Capability List와 ACL 등이 있다.

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: Capability List와 ACL은 접근통제 행렬 기반의 임의적 접근통제 메커니즘에 가깝다.

</details>

### 147. 다음 중 역할 기반 접근제어의 기본 보안정책과 가장 거리가 먼 것은?

1. 권한의 최소화
2. 직무의 분리
3. 데이터 추상화
4. 다단계 보안 등급

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: RBAC의 핵심은 역할을 통한 권한 관리, 최소 권한, 직무 분리, 데이터 추상화다. 다단계 보안 등급은 MAC/BLP 계열과 더 가깝다.

</details>

### 154. 이해 충돌이 발생할 수 있는 상업용 응용을 위해 개발되었고, 자유재량과 강제적 접근 개념을 이용하였으며, 정보흐름 모델을 기반으로 한 보안 모델은?

1. 만리장성 모델
2. 벨라파듈라 모델
3. 비바 무결성 모델
4. 클락-월슨 무결성 모델

<details>
<summary>정답 보기</summary>

**정답**: 1

**근거**: 만리장성 모델, 즉 Chinese Wall/Brewer-Nash 모델은 이해상충 방지를 위한 상업적 접근통제 모델이다.

</details>

## 1과목 시스템 보안

### 166. 다음 지문에서 설명하는 파일은?

> 이 파일은 미디어 장치의 루트 디렉터리에 위치하며 미디어 연결 시 특정 프로그램이 자동으로 실행되도록 제어한다. 해당 미디어가 악성코드에 감염되었을 때 감염 확산 또는 정보 유출 등의 피해가 발생할 수 있다.

1. mediarun.msc
2. activerun.inf
3. autorun.inf
4. execute.inf

<details>
<summary>정답 보기</summary>

**정답**: 3

**근거**: 이동식 미디어 루트에 위치하여 자동 실행을 제어하는 대표 파일은 `autorun.inf`다.

</details>

### 172. 인터넷 익스플로러에서는 보안 설정을 지정할 웹 콘텐츠 영역을 지정할 수 있다. 다음 중 지정할 수 없는 영역은 어느 것인가?

1. 로컬 인트라넷
2. 신뢰할 수 있는 사이트
3. 보안 등급 지정 가능한 사이트
4. 제한된 사이트

<details>
<summary>정답 보기</summary>

**정답**: 3

**근거**: Internet Explorer 보안 영역은 인터넷, 로컬 인트라넷, 신뢰할 수 있는 사이트, 제한된 사이트로 구분된다.

</details>

### 174. 사용자 PC가 언제 부팅되었는지를 확인하기 위해 입력해야 하는 명령어는?

1. `net statistics workstation`
2. `net computer boot time`
3. `net reboot time`
4. `net time boot`

<details>
<summary>정답 보기</summary>

**정답**: 1

**근거**: Windows에서 `net statistics workstation` 출력의 Statistics since 항목으로 부팅 이후 통계 시작 시각을 확인할 수 있다.

</details>

### 186. 윈도우 NTFS의 파일 및 디렉터리에 대한 보안 규칙 중 알맞지 않은 것은?

1. 접근 권한은 누적된다.
2. 부모 디렉터리의 권한이 하위 디렉터리 및 파일에 상속된다.
3. 허용보다는 거부가 우선이다.
4. 디렉터리에 대한 접근 권한이 파일에 대한 접근 권한보다 우선한다.

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: NTFS 권한에서는 명시적 권한과 파일 단위 권한이 우선적으로 적용될 수 있으며, 디렉터리 권한이 항상 파일 권한보다 우선하는 것은 아니다.

</details>

### 190. 윈도우 레지스트리의 주요 하이브 하위 키는 여러 개가 있다. 다음 중 시스템 및 원격 로그인을 진행할 때 윈도우 시스템이 파일을 참조하여 인가되지 않은 사용자 접근을 제한할 경우 이용하는 파일은?

1. SECURITY
2. HOST
3. SYSTEM
4. SAM

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: SAM(Security Account Manager)은 로컬 계정과 패스워드 정보를 관리하며 Windows 로그온 인증 흐름에서 참조된다.

</details>

### 197. 서버의 파일 시스템에 대한 시간 속성이 잘못 기술된 것은?

1. mtime: 파일을 생성하거나 내용을 수정한 시간
2. atime: 최근 파일을 읽거나 실행시킨 시간
3. ctime: 파일 속성이 변경된 시간
4. rtime: 파일을 실시간 실행시킨 시간

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: Unix/Linux 파일 시간 속성은 일반적으로 atime, mtime, ctime으로 구분한다. rtime은 표준 파일 시간 속성이 아니다.

</details>

### 209. 다음 중 `/etc/passwd` 파일의 각 리눅스 계정에서 사용자 정의 셸을 제거하기 위한 변경 값으로 적절한 것은?

1. `/bin/false`
2. `/bin/nosh`
3. `/bin/nobash`
4. `/bin/disable`

<details>
<summary>정답 보기</summary>

**정답**: 1

**근거**: 로그인 차단용 셸로 `/bin/false` 또는 `/sbin/nologin`을 사용한다. 보기 중 적절한 값은 `/bin/false`다.

</details>

### 214. 리눅스 시스템에서 파일의 접근권한을 확인하였더니 `rwSr--r--`이었다. 권한 내의 대문자 `S`에 대한 설명으로 옳은 것은?

1. SetUID를 실행 권한이 없는 파일에 설정할 경우 대문자 `S`로 표기된다.
2. SetGID를 설정하여 대문자 `S`로 표기하였다.
3. 디렉터리에 스티키 비트가 설정되어 대문자 `S`로 표기하였다.
4. SetGID를 실행 권한이 없는 파일에 설정할 경우 대문자 `S`로 표기된다.

<details>
<summary>정답 보기</summary>

**정답**: 1

**근거**: 소유자 실행 권한 위치에 대문자 `S`가 있으면 실행 권한 없이 SetUID만 설정된 상태를 의미한다.

</details>

### 220. TCP-Wrapper는 네트워크 서비스와 관련한 트래픽을 제어하고 모니터링할 수 있는 방화벽 툴이다. 외부의 연결로부터 보호할 네트워크 서비스들을 선택하여 `tcpd`가 보호하도록 설정 파일 수정이 필요한데, 이때 수정이 이루어지는 설정 파일은?

1. `/etc/inetd.conf`
2. `/etc/shadow`
3. `/etc/hosts.equiv`
4. `/etc/fstab`

<details>
<summary>정답 보기</summary>

**정답**: 1

**근거**: `inetd` 기반 서비스에서 TCP-Wrapper를 적용하려면 `/etc/inetd.conf`에서 대상 서비스를 `tcpd` 경유로 실행하도록 설정한다.

</details>

### 227. 다음 중 유닉스 서버 시스템 최적화를 위한 파일 관리 방안들에 대한 설명으로 잘못된 것은?

1. 소유자가 존재하지 않는 파일 및 디렉터리가 존재한다면 필요에 따라 소유자 및 그룹을 변경하거나 삭제해야 한다.
2. root 계정의 PATH 환경변수에 현재 디렉터리를 의미하는 `.`이 필요하다면 맨 마지막으로 이동하여 포함시켜야 한다.
3. 특정 디렉터리 내에 `.`으로 시작하는 숨겨진 파일이 존재한다면 소유자에게 확인하고 불필요한 경우 삭제해야 한다.
4. `hosts.equiv` 파일 소유자를 root로 설정하고 해당 파일에 `+` 설정을 포함하여 필요한 호스트 및 계정만 사용할 수 있도록 설정해야 한다.

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: `hosts.equiv`의 `+` 설정은 신뢰 호스트 범위를 과도하게 넓혀 보안상 위험하므로 사용하지 않아야 한다.

</details>

### 230. 윈도우 2008 서버에서의 로그 관리는 이벤트 뷰어라는 관리 도구를 사용한다. 다음에 설명하는 이벤트는 무엇인가?

> 파일이나 다른 개체 만들기, 열기, 삭제 등의 리소스 사용과 관련된 이벤트이다. 로그인 시도 및 잘못된 로그인 시도 이벤트이며 이벤트 유형은 관리자가 지정한다.

1. 응용 프로그램 및 로그
2. 보안 로그
3. 시스템 로그
4. 리소스 로그

<details>
<summary>정답 보기</summary>

**정답**: 2

**근거**: 객체 접근, 로그온 성공·실패, 감사 정책과 관련된 이벤트는 Windows 보안 로그에 기록된다.

</details>

### 239. 다음 중 무결성 검사를 위한 도구가 아닌 것은?

1. tripwire
2. Fcheck
3. Samhain
4. prstat

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: Tripwire, Fcheck, Samhain은 파일 무결성 점검 도구다. `prstat`는 Solaris 계열의 프로세스 상태 확인 도구다.

</details>

### 240. 공개소스 보안도구인 Nessus의 기능을 가장 잘 표현하고 있는 것은?

1. 포트 스캐너
2. 침입차단시스템
3. 침입탐지시스템
4. 네트워크 기반 취약점 스캐너

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: Nessus는 네트워크 기반 취약점 스캐너로 분류된다.

</details>

### 257. 다음 지문에 해당하는 특징을 갖는 공개 해킹 도구에 적합한 용어는?

> 이 도구의 목적은 자신과 다른 소프트웨어를 보이지 않게 숨기고 사용자가 공격자의 소프트웨어를 인지하고 제거할 가능성을 피하는 것이다. 이 도구에는 키로거, 봇넷 및 재전송 메일을 포함해 거의 모든 소프트웨어를 숨길 수 있다.

1. 매크로 바이러스
2. Back Office
3. 루트킷
4. 은폐형 바이러스

<details>
<summary>정답 보기</summary>

**정답**: 3

**근거**: 루트킷은 악성 프로세스, 파일, 네트워크 연결 등을 숨겨 탐지를 회피하는 도구 또는 기법이다.

</details>

### 709. 다음 문장에서 설명하는 것은?

> HTML 문서 내에서 링크를 클릭했을 때 새롭게 열린 탭 페이지에서 기존 문서의 `location`을 피싱 사이트로 변경해서 정보를 탈취하는 공격 기술을 말한다. 이 공격은 메일이나 SNS와 같은 오픈 커뮤니티에서 사용될 수 있다. 사용자의 클릭을 유도하여 웹 브라우저의 탭을 피싱 사이트로 이동시키는 기존의 피싱 기법과 달리 사용자가 페이지에서 아무런 행위를 하지 않아도 사용자의 눈을 피해 열린 탭 중 하나를 피싱 페이지로 로드한다.

1. 탭내빙(Tabnabbing)
2. 클릭재킹(Clickjacking)
3. 파밍(Pharming)
4. 스미싱(Smishing)

<details>
<summary>정답 보기</summary>

**정답**: 1

**근거**: 열린 브라우저 탭의 위치를 피싱 페이지로 바꾸어 사용자를 속이는 공격은 탭내빙(Tabnabbing)이다.

</details>

### 711. 다음 중 암호키 보호에 하드웨어를 사용하는 기술과 가장 거리가 먼 것은?

1. 스마트 카드
2. HSM
3. TPM
4. SIM

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: 스마트 카드, HSM, TPM은 암호키 보호와 암호 연산을 위한 보안 하드웨어로 자주 분류된다. SIM은 이동통신 가입자 인증 모듈로, 일반적인 암호키 보호 기술 분류와는 거리가 있다.

</details>

### 755. 다음 중 윈도우의 관리 도구와 해당 도구를 실행하기 위한 명령어로 잘못 짝지어진 것은?

1. 방화벽 - `firewall.cpl`
2. 서비스 - `services.msc`
3. 인터넷 속성 - `inetcpl.cpl`
4. 로컬 보안 설정 - `fsmgmt.msc`

<details>
<summary>정답 보기</summary>

**정답**: 4

**근거**: 로컬 보안 정책은 `secpol.msc`로 실행한다. `fsmgmt.msc`는 공유 폴더 관리 도구다.

</details>
