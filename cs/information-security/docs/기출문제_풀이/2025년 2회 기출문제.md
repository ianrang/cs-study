# 2025년 제2회 정보보안기사 필기 복원문제

| 항목 | 내용 |
|------|------|
| 성격 | 사용자 작성 복원문제 기반 검수본 |
| 확보 문항 | 15문항 |
| 누락 번호 | 9, 16, 17, 18, 19 |
| 작성 원칙 | 기존 문항 번호·보기·정답 매핑을 보존하되, 개념 검증상 정답 단정이 어려운 문항은 확인 필요로 표시한다. |
| 주의 | 현재 파일은 1과목 시스템 보안 일부 복원문제이며, 100문항 완전 복원본이 아니다. |

## 1과목 시스템 보안

### 1. 리눅스 명령어 중에서 시스템에서 열려 있는 파일을 확인하는 명령어는 무엇인가?

1. ps -ef
2. file
3. lsof
4. vmstat
<details>
<summary>정답 보기</summary>

**정답**: 3

</details>

### 2. 리눅스 프로세스 중에서 좀비 프로세스를 탐지하는 명령어로 가장 적절한 것은?

1. ps -ef | grep zombie
2. ps -ef | grep defunct
3. top | grep zombie
4. top | grep defunct
<details>
<summary>정답 보기</summary>

**정답**: 2

</details>

### 3. 다음 중 Kali Linux의 기본 Top 10 도구에 포함되어 있지 않은 것은?

1. Nmap
2. metasploit
3. dirsearch
4. Burp Suite
<details>
<summary>정답 보기</summary>

**정답**: 3

**검수 상태**: 복원 보정

**검수 메모**: 기존 보기의 `OWASP ZAP`은 Kali 공식 도구 페이지에서 기본 Top 10이 아니라 `large/everything` 계열로 분류되어, `dirsearch`와 함께 복수 정답 논란이 있었다. 단일정답 문항으로 만들기 위해 Kali `kali-tools-top10`에 포함되는 `Burp Suite`로 보기를 보정했다. `kali-tools-top10`에는 `nmap`, `metasploit-framework`, `burpsuite`가 포함되고 `dirsearch`는 포함되지 않는다.

</details>

### 4. 다음 중 실행 파일에 대해서 정적 분석과 동적 분석을 모두 수행할 수 있는 리버싱 도구는?

1. Process Explorer
2. Process Monitor
3. IDA Pro
4. Autoruns
<details>
<summary>정답 보기</summary>

**정답**: 3

</details>

### 5. 다음 중 사용자가 작성한 코드에 없는 함수를 호출하고자 할 때 이미 적재된 공유 라이브러리의 함수를 사용하는 기법으로, NX bit나 DEP를 우회하는 데 악용될 수 있는 것은?

1. ASLR
2. RTL (Return-to-libc)
3. PIE
4. RELRO
<details>
<summary>정답 보기</summary>

**정답**: 2

</details>

### 6. 리눅스 `/etc/passwd` 파일의 3번째 필드는 무엇인가?

> daemon:x:1:1:daemon:/usr/sbin:/bin/sh

1. password
2. UID
3. GID
4. Shell
<details>
<summary>정답 보기</summary>

**정답**: 2

</details>

### 7. 다음 `ls -al` 명령어 출력에서 `+`의 의미로 올바른 것은?

> -rw-rw-r--+ 1 root root 3826 Sep 23 2020 web.txt

1. Encryption
2. ACL (Access Control List)
3. Read Only file
4. Hidden file
<details>
<summary>정답 보기</summary>

**정답**: 2

</details>

### 8. 다음 중 X Client 원격 접속 시 root 사용자 제한을 위한 명령어와 설정 파일의 조합으로 올바른 것은?

1. `xhost -`, `$XAUTHORITY`
2. `xhost -`, `/root/.Xauthority`
3. `xauth -`, `$XAUTHORITY`
4. `xauth -`, `/root/.Xauthority`
<details>
<summary>정답 보기</summary>

**정답**: 2

**검수 메모**: 기존 보기의 `xhost - /root/.Xauthority`는 하나의 명령처럼 읽히면 부정확하므로, 명령어 `xhost -`와 root 계정의 X 인증 파일 `/root/.Xauthority`의 조합으로 표현을 분리했다.

</details>

### 10. 웹브라우저의 확장 기능으로 설치되어 웹브라우저와 웹서버 사이에서 웹 트래픽을 가로채어 수정할 수 있는 공격은?

1. Sniffing
2. MitB
3. Blueprinting
4. Bluejacking
<details>
<summary>정답 보기</summary>

**정답**: 2

</details>

### 11. 다음 중 BOF(Buffer Overflow) 공격에 취약한 API는?

1. strncpy
2. gets
3. fgets
4. strncat
<details>
<summary>정답 보기</summary>

**정답**: 2

</details>

### 12. 다음 중 무결성 검사 도구가 아닌 것은?

1. Tripwire
2. AIDE
3. Samhain
4. SPAN
<details>
<summary>정답 보기</summary>

**정답**: 4

</details>

### 13. 다음은 FTP에 대한 설명으로 올바르지 않은 것은?

1. FTP는 명령어 전송을 위해서 21번 포트를 사용하고 있다.
2. FTP에 관련된 로그 파일은 xferlog 파일이다.
3. Active 모드의 경우 데이터 전송을 위해서 20번 포트를 사용한다.
4. Passive 모드의 경우 데이터 전송을 위해서 서버가 0~1,023번 포트를 결정한다.
<details>
<summary>정답 보기</summary>

**정답**: 4

</details>

### 14. 다음은 2025년 가트너 10대 전략 기술 트렌드가 아닌 것은?

1. Agentic AI
2. AI 거버넌스 플랫폼
3. Bigdata
4. 허위 정보보안 (Disinformation Security)
<details>
<summary>정답 보기</summary>

**정답**: 3

</details>

### 15. 다음 중 드론 보안 취약점 중에서 가용성 방해와 관련 없는 것은?

1. GPS 재밍
2. 제어신호 전파 방해
3. 재전송 공격
4. 센서 교란
<details>
<summary>정답 보기</summary>

**정답**: 3

</details>

### 20. 윈도우 인증 시에 윈도우 계정과 SID를 매칭하고 감사로그를 기록하는 것은?

1. LSA
2. SAM
3. SRM
4. Winlogon
<details>
<summary>정답 보기</summary>

**정답**: 3

</details>
